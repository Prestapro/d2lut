#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd -- "$(dirname -- "${BASH_SOURCE[0]}")" >/dev/null 2>&1 && pwd)"

# Backward-compatible wrapper. Universal checks are config-driven via .opencode/pipeline.json.
exec "${SCRIPT_DIR}/opencode-universal-checks" "$@"
